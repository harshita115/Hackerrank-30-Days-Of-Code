#include <bits/stdc++.h>

using namespace std;

int main()
{
    int tip, tax;
    double mealcost,tipP,taxP,total;
    cin>>mealcost;
    cin>>tip;
    cin>>tax;
    tipP=(mealcost*tip/100);
    taxP=(mealcost*tax/100);
    total=round(mealcost+tipP+taxP);
    cout<<total;
    return 0;
}
